{
  "name": "titan-nova-codex",
  "version": "1.0.0",
  "private": true,
  "description": "Titan Nova five-bot multi-session runtime launcher.",
  "main": "whatsapp_multi_session.js",
  "scripts": {
    "start": "node whatsapp_multi_session.js",
    "gateway": "node whatsapp_multi_session.js",
    "check": "node --check whatsapp_multi_session.js && node --check gateway_codex_preflight_patch.js && node --check multi_session_manager.js && node --check redis_auth_state.js && for f in bot/*.js; do node --check \"$f\"; done"
  },
  "dependencies": {
    "@whiskeysockets/baileys": "6.7.23",
    "axios": "1.18.1",
    "express": "4.22.2",
    "pino": "9.6.0",
    "qrcode-terminal": "0.12.0",
    "redis": "4.7.0"
  }
}
