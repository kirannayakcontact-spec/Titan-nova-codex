# Legacy Backup

This folder keeps the old Titan Nova monolithic runtime files as a backup only.
They are no longer active entrypoints.

Active new work starts from:

```text
```

Backed up files:

- `flask_app.py.bak` — old Flask monolith
- `Gateway.js.bak` — old Node/WhatsApp gateway monolith
