# Deploy

Deployment notes and future deploy scripts live here.

Current production deploy remains:

```bash
titan
```

or:

```bash
cd ~/titan-app && ./titan_one_command.sh
```

Do not delete the root `titan_one_command.sh` until Termux deploy has been migrated and tested.
